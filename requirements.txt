# Full install — use requirements-ml.txt (includes core + ML pipelines)
# For Python 3.14 on Windows, prefer: pip install -r requirements-core.txt
# then pip install -r requirements-ml.txt
-r requirements-ml.txt
